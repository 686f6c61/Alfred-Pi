/* Diario de la casa: modal con el CHANGELOG completo. Generado desde main:CHANGELOG.md. */
(function () {
  var CHANGELOG_HTML = "<section class=\"cl-version unreleased\"><h3>Sin publicar</h3><h4>Corregido</h4>\n<ul><li><b>El relevo no llegaba a dispararse nunca.</b> El conteo de fallos vivía en <code>after_provider_response</code>, y pi solo emite ese evento cuando hay respuesta HTTP: un rechazo de conexión, un DNS, un timeout o un 5xx nunca llegan a él, así que el contador no pasaba de cero y el relevo era inalcanzable (verificado contra pi 0.84.3 con un proveedor real caído). El conteo pasa a <code>turn_end</code>, que sí se emite en todo cierre de turno: `stopReason: \"error\"<code> cuenta (HTTP o transporte por igual), </code>aborted` es neutro y el resto limpia la racha. Nueva guard <code>fallback-turn-outcome.test.ts</code> para que el conteo no vuelva a cablearse a un evento que no ve fallos.</li><li><code>--alfred-pi=autopilot</code> y <code>domains</code> imprimen texto legible; el sufijo <code>:json</code> da el objeto para máquinas, como ya hacía <code>stack</code> y como prometía <code>docs/comandos.md</code>. Antes ambas variantes imprimían el mismo JSON crudo.</li><li><code>recordTransportFailure</code> y <code>classifyTransportFailure</code> eran código muerto (nadie los invocaba en runtime); se retiran junto a <code>recordResponse</code>.</li></ul>\n<h4>Documentación</h4>\n<ul><li>README ES/EN: la frase del sitio público ya no dice que <code>www/</code> está en este checkout (vive en la rama <code>landing</code>; <code>main</code> solo guarda el HTML plano de respaldo en <code>site/</code>). La sección de seguridad nombra el host del canal de actualizaciones (<code>pi.686f6c61.dev</code>), que ya estaba en la nota de privacidad.</li><li><code>package.json</code> pierde los scripts <code>docs:site*</code>: apuntaban a <code>www/</code>, que no existe en <code>main</code>, y fallaban en cualquier checkout.</li><li><code>arquitectura.md</code>, <code>pi.md</code>, <code>modulos.md</code>, <code>probar.md</code> y <code>comandos.md</code> reflejan el nuevo evento de conteo y el reparto texto/JSON del flag.</li></ul></section><section class=\"cl-version\"><h3>0.4.0</h3><h4>Packs</h4>\n<ul><li><code>clean-code</code> recomienda <code>@dietrichgebert/ponytail</code> (escalera YAGNI: stdlib y dependencias antes de inventar). Inyecta reglas cada turno; aviso en el contexto del pack; instalación solo por <code>/packages</code>.</li><li><code>qa-testing</code> gana <code>bug-repro-loop</code>: mando rojo de un bug duro (comando que ya falló, hipótesis, prueba de regresión). No es un 502 ni un flake.</li><li><code>ai-agents</code> gana <code>/implement</code> y el patrón DAG con frontera (un worktree por escritor, un colector). Si no hay aristas bloqueantes, sigue <code>/fanout</code>.</li><li><code>pr-review-checklist</code> gana el eje Spec (fidelidad al encargo, distinto de la corrección).</li><li>El catálogo pasa a 53 skills y 27 prompts. Siguen once packs.</li><li>Se retiran del árbol los directorios vacíos <code>docker-hardening</code> y <code>hardening-checklist</code>, herencia de dos fusiones anteriores.</li></ul>\n<h4>Harness</h4>\n<ul><li>Esenciales: <code>pi-context-view</code> (<code>/context</code>, mapa de contexto sin inyectar instrucciones) y <code>@narumitw/pi-btw</code> (<code>/btw</code>, hilo al margen).</li><li><code>--alfred-pi=usage:N</code> acota el informe a los últimos N días. El parser del rango existía desde 0.3.0, pero la guarda del valor lo dejaba inalcanzable; ahora responde y tiene caso en el viaje headless.</li><li>La TUI deja de ofrecer referencias <code>!comando</code> al pedir una clave. El resolutor siempre las rechazó, así que la etiqueta invitaba a guardar una credencial que jamás resolvería.</li></ul>\n<h4>Documentación</h4>\n<ul><li>Pasada de contraste de los diez documentos de <code>docs/</code> y los dos README contra el código 0.4.0. Se corrigen la frontera de <code>lib/</code> (falta <code>onboarding-flow.ts</code> como importador de pi), el diagrama de arranque, el contrato de frontmatter de los prompts y el manifiesto de pack.</li><li>La nota de privacidad y el apartado de seguridad dejan de decir que la única salida de red es la que dispara el usuario: se nombran models.dev, el registro de npm y el canal de actualizaciones.</li></ul>\n<h4>Sitio</h4>\n<ul><li>Cifras 53/27 en casa, changelog, <code>llms.txt</code> y las imágenes de compartir.</li><li><code>llms.txt</code> y <code>llms.es.txt</code> dejan de ser una ficha de viñetas y pasan a brief de producto: desambiguación de nombres, superficie de comandos, las once salas con su disparador, los cinco mecanismos, qué sale a la red y una lista expresa de lo que el producto no hace.</li><li>Docs técnicos reescritos en compilación: los enlaces <code>.md</code> ya no dependen de JavaScript y las tablas anchas van envueltas.</li><li>Accesibilidad y paridad: enlace de salto, contraste de texto, encabezados, gutter bilingüe, FAQ inglesa a la par y una sola fuente para el JSON-LD.</li></ul></section><section class=\"cl-version\"><h3>0.3.0</h3><h4>El nombre</h4>\n<p>pi-harness-moe pasa a llamarse Alfred-Pi en público: npm <code>alfred-pi</code>,</p>\n<p>flag headless <code>--alfred-pi</code> (con <code>--harness-moe</code> como alias que avisa</p>\n<p>durante un ciclo), directorio de estado <code>~/.pi/agent/alfred-pi/</code> y</p>\n<p>statusline <code>alfred</code> / <code>alfred-sala</code> / <code>alfred-presupuesto</code>. Alfred es</p>\n<p>la casa; pi sigue siendo el anfitrión y harness.moe el estudio. Una</p>\n<p>instalación 0.2.x encuentra su estado tras actualizar: el primer</p>\n<p>arranque copia el directorio viejo al nuevo y deja constancia en</p>\n<p><code>migrated-from.json</code>. El repositorio de GitHub pasa a</p>\n<p><code>686f6c61/Alfred-Pi</code>; las URL de instalación apuntan ahí. No se</p>\n<p>publica npm en este cubo.</p>\n<h4>El sitio</h4>\n<p>Nace la documentación en Astro, bilingüe (español en <code>/</code>, inglés en</p>\n<p><code>/en/</code>), hecha a medida por el estudio. Importa los <code>docs/</code> públicos</p>\n<p>como colección en lugar de duplicarlos. Las salas se explican en</p>\n<p>prosa, la FAQ responde preguntas reales y <code>docs/auditoria/</code> sigue sin</p>\n<p>publicarse. El generador HTML plano deja de ser la cara pública;</p>\n<p><code>site/manifest.json</code> se conserva como canal de actualizaciones. El</p>\n<p>sitio publica mapa del sitio con hreflang, robots, imagen para</p>\n<p>compartir (Open Graph y Twitter) y datos estructurados.</p>\n<h4>Cubo Ahora</h4>\n<p>La casa de siempre, asentada: gobierno de proveedores y claves sobre</p>\n<p>los archivos nativos de pi, doctor con sondas reales, asistente de</p>\n<p>primer arranque y el escudo de escrituras (plan, diff, backup,</p>\n<p>atómica).</p>\n<h4>Cubo Siguiente</h4>\n<p>Intenciones, esenciales por tramo, watchdog, memoria opt-in, local</p>\n<p>first y la lengua de casa (pack, paquete, turno, presupuesto, relevo,</p>\n<p>clave, perfil). El presupuesto lee sesiones locales, avisa y no corta.</p>\n<p>El relevo salta por fallo, no por precio.</p>\n<h4>Packs</h4>\n<ul><li>Profundizado <code>web-fullstack</code> (siguen 11 packs): skills <code>http-service</code>, <code>app-persistence</code>, <code>async-jobs</code> y <code>browser-improve</code>. Capturas y SEO no se clonan; <code>seo-analytics</code> gana límites y <code>visual-guides</code> gana anti-patrones. El catálogo pasa a 52 skills. El directorio vacío <code>deploy-checklist</code> se retira.</li><li>Reescritas las skills de seguridad web y fullstack de la oleada S-SKL-W1. <code>release-gate</code> sustituye las listas gemelas de endurecimiento y despliegue, aporta evidencia ejecutable y reduce el catálogo a 49 skills. Los contexts de ambos packs pasan de lemas a restricciones verificables.</li><li>Reescritas las skills de conversión, copy y crítica visual de <code>landing-design</code>. <code>docker-workflow</code> absorbe el endurecimiento de contenedores, se retira su gemela y el catálogo queda en 48 skills. Las skills operativas de DevOps ganan verificaciones, formatos y límites sin duplicar su oficio.</li><li>Reescritas las skills de dashboard, calidad de datos, estrategia de pruebas, factorías y traducción de la oleada S-SKL-W4. <code>pandas-analysis</code> gana una puerta de revisión acotada; los contexts de datos y agentes pierden metáforas y comprimen sus restricciones sin crear skills nuevas.</li><li>Recomendados reauditados en npm: devops gana <code>@danypops/pi-pipes</code>, QA gana <code>tdd-enforcer</code>, clean-code gana <code>@plannotator/pi-extension</code> y ai-agents gana <code>pi-memory</code> (aviso de secretos; solo por <code>/packages</code>). Compliance sigue sin recomendados. Test guardián impide resucitar los seis paquetes muertos de N-PCK-01.</li></ul>\n<h4>Docs</h4>\n<ul><li>Documentación pública en Astro (<code>www/</code>), bilingüe. <code>docs/auditoria/</code> no entra. <code>bun run docs:site</code> construye <code>www/dist/</code>.</li><li>Mapa de desarrollador: <code>comandos.md</code>, <code>extender.md</code> y <code>probar.md</code>. Arquitectura documenta <code>curateTurn</code>, sala, intenciones, memoria opt-in y watchdog. <code>modulos.md</code> lista los siete módulos del cubo Siguiente. <code>instalacion.md</code> deja de ofrecer <code>!comando</code> (el harness lo rechaza). <code>dominios.md</code> pasa a «Packs de trabajo» y describe el manifiesto.</li><li>Cifras públicas (tests, packs, skills, prompts, presets) salen del árbol; el README deja de copiar una cifra muerta.</li><li>Lengua pública: pack, paquete, turno, presupuesto, relevo, clave, perfil. El local no se llama sótano. La mansión queda como explicación, no como menú de comandos.</li><li>Primeros minutos: el asistente de primer arranque, no solo <code>/providers</code> y <code>/reload</code>.</li><li>Auditoría en esenciales; el buscador <code>/packages</code> se alinea (N-ESC-02).</li><li>El guardián del presupuesto explica en el primer arranque y en su ficha que lee las sesiones locales y avisa, sin cortar ni enviar datos a ningún sitio (P-28).</li></ul></section><section class=\"cl-version\"><h3>0.2.0</h3><h4>Domain expansion</h4>\n<ul><li>22 new skills: 15 across existing packs (secret-scanning, dependency-audit, api-reference, traduccion-en-es, web-performance, frontend-security, ab-testing, seo-analytics, kubernetes-triage, observabilidad, db-ops, pr-review-checklist, tech-debt-inventory, ddd-architecture plus the earlier design-systems) and the /fix-findings prompt.</li><li>Two new packs: data-analisis (sql-optimization, pandas-analysis, dashboard-design, data-quality; /query-review /explore /dashboard) and qa-testing (test-strategy, fixtures-factories, contract-testing, flaky-hunting; /test-plan /flaky /coverage-gaps). Totals: 11 packs, 49 skills, 26 prompts.</li></ul>\n<h4>Product depth</h4>\n<ul><li>Headless modes --harness-moe=stack|autopilot|domains with :json output; stack info extracted to lib/stack.ts so TUI and headless share source.</li><li>Provider error classifier (cause, action, retry policy) behind the doctor and failover notes instead of bare status codes.</li><li>First-run onboarding wizard (guided preset, key, probe, default, autopilot, budget) when no providers are configured; dismissable, never repeats.</li><li>Community integrations recommended per pack: -tech/pi-changelog, @narumitw/pi-github-pr, pi-secrets-guard, @testzugang/pi-dependency-audit.</li></ul></section><section class=\"cl-version\"><h3>0.1.0</h3><p>Initial release.</p>\n<h4>Provider control center (<code>/providers</code>)</h4>\n<ul><li>Full TUI over pi's native <code>models.json</code>, <code>auth.json</code> and <code>settings.json</code> no parallel config, no drift.</li><li>Safe writes: unified-diff preview (colored), atomic writes, automatic timestamped backups (retention 10, pinnable, restorable), live reload without restarting pi.</li><li>Wizard with 22 provider presets: xAI Grok / Grok Code, Moonshot Kimi (OpenAI + Anthropic-compatible), OpenAI Codex (Responses API), Anthropic Claude, z.ai GLM (API + Coding Plan), Ollama Cloud (remote <code>/v1</code>), OpenRouter, DeepSeek, Groq, Together, Mistral, Cerebras, Fireworks, and own inference servers (Ollama, LM Studio, vLLM, SGLang, LiteLLM, or any custom OpenAI-compatible server).</li><li>Model discovery via <code>GET /models</code> with a multi-select checklist; manual entries always survive re-discovery.</li><li>Metadata autofill from models.dev (context window, output limit, reasoning, vision, $/M pricing - ollama <code>:cloud</code> models included); fills only missing fields, 24h cache, silent offline degradation.</li><li>Keys with hygiene: literals, <code>$ENV_VAR</code>, <code>!command</code> refreshers; masked display; inline resolution check.</li><li>Defaults management with live session switch (<code>setModel</code> + thinking level).</li></ul>\n<h4>Doctor</h4>\n<ul><li>Free liveness probes per provider (latency + model list), actionable errors (auth vs quota vs URL), deep 1-token probes on demand.</li><li>Config reconciliation: trailing slashes, missing api types, stale defaults, unset <code>$ENV</code>, broken <code>!command</code>, duplicate model ids.</li><li>Health history in <code>~/.pi/agent/pi-harness-moe/health.jsonl</code> with per-provider success rate and latency.</li><li>Headless: <code>pi --harness-moe=doctor</code>.</li></ul>\n<h4>Profiles &amp; auto-fallback (<code>/profile</code>)</h4>\n<ul><li>Named model stacks - ordered <code>{provider, model, thinkingLevel?}</code> chains; applying picks the first step that resolves.</li><li>Auto-fallback: after 2 consecutive provider failures on the active model, switches to the next healthy step before the next turn (between turns only, counters reset on success).</li></ul>\n<h4>Autopilot (<code>/autopilot</code>)</h4>\n<ul><li>Per-turn domain detection cascade: prompt triggers (Spanish and English, word-boundary scoring, long triggers weigh more) → repo hints (Dockerfile → devops, sonar config → security, astro config → web…) → sticky last domain for follow-up turns. Focused context injection - only the detected domain's context goes into the system prompt, statusline shows the <code>dom:</code> badge.</li><li>Optional <code>context+thinking</code> routing applies the domain's recommended thinking level per turn.</li><li>\"Deal all cards\": one action enables every pack's skills and prompts; autopilot focuses contexts, the model keeps the full skills menu.</li><li>Off by default; manual stacked-context mode remains the default behavior.</li></ul>\n<h4>Domain packs (<code>/domains</code>)</h4>\n<ul><li>Themed work-area bundles of skills, prompt templates, injected system-prompt context and a recommended model posture.</li><li>Nine packs: security (incl. SonarQube audits on a local Docker server), <b>ai agents</b> (orchestration patterns, role/model assignment, budgets, /fanout), <b>docs</b> (Diátaxis structure, ADRs, /docs-audit), **escritura ES** (RAE orthography reference, /revision-es, zero-emoji policy), clean code, web/fullstack (incl. Astro and visual step-by-step guides with screenshots), compliance (privacy, licensing, a11y), landing design (vision critique), devops/infra (incl. docker workflow, GitHub Actions CI with trusted publishing, and hardening).</li><li>Emoji-free by policy: interface, docs and output use typographic symbols only (typographic marks such as checkmarks are not emojis); no emojis anywhere unless explicitly requested.</li><li>Enable per project or globally via symlinks with strict ownership checks disabling removes only links this extension created.</li><li>Recommended community packages per pack (e.g. pi-sonar, piolium, pi-browser-harness, the Astro docs MCP) shown in the TUI with manual install commands.</li></ul>\n<h4>Essentials with security audit (<code>/essentials</code>)</h4>\n<ul><li>Curated parity packages (MCP adapter, subagents, plan mode, permissions, web access, todos, LSP feedback, powerline footer) with one-shot \"install all missing\". Nothing installs silently.</li><li>Pre-install security audit: fetches the package's public sources and scans for risky patterns - fetch-and-execute, credential exfiltration, decoded payloads feeding eval, pipe-to-shell installers, whole-env serialization, minified network bundles - plus the network endpoints it references.</li></ul>\n<h4>Usage &amp; cost (<code>/usage</code>)</h4>\n<ul><li>Offline, ccusage-style reports per model, day and top sessions, parsed from local session JSONL files; pricing from models.json <code>cost</code> fields (or autofill); unpriced models flagged. Headless: <code>pi --harness-moe=usage</code>.</li></ul>\n<h4>Ollama manager (<code>/ollama</code>)</h4>\n<ul><li>Talks to the local ollama server API directly: installed models (local + <code>:cloud</code> side by side), running processes, pull with live progress, remove, one-key register into pi (cloud models get <code>_launch</code>).</li></ul>\n<h4>Package browser &amp; security audit (<code>/packages</code>)</h4>\n<ul><li>Search the whole pi ecosystem (npm, keyword <code>pi-package</code>) with monthly downloads, package details (version, license, readme), one-key install.</li><li>Security audit before installing - for npm sources (fetched via unpkg) and <b>git sources</b> (shallow-cloned into a temp dir): fetch-and-execute, credential exfiltration, decoded payloads feeding eval, pipe-to-shell installers, whole-env serialization, minified network bundles, plus the network endpoints referenced.</li></ul>\n<h4>Stack (<code>/stack</code>)</h4>\n<ul><li>Control tower: active model + thinking, defaults, autopilot state + last detected domain, auto-fallback profile, enabled domains and available skills/prompts, installed packages, daily budget and per-provider health.</li></ul>\n<h4>Daily budget guard</h4>\n<ul><li>Set a max USD/day in <code>/usage</code> - the statusline shows budget usage, warns at 80% (once per day), and at 100% injects a frugality note into the system prompt (tight answers, no fleet launches without asking). Observes and nudges; never silently blocks.</li></ul>\n<h4>Project documentation</h4>\n<ul><li>Technical documentation under <code>docs/</code>: architecture, modules, domains, install, data and config. Architecture decisions are kept outside the repo, not as in-tree ADRs.</li><li>CONTRIBUTING.md (setup, ground rules, how to add packs/presets) and SECURITY.md (policy, what we commit to, explicit non-goals).</li></ul>\n<h4>Persona and startup header</h4>\n<ul><li>Startup header in the TUI: \"pi-harness-moe · un producto de harness.moe · desarrollado por @686f6c61\" plus a two-line pitch with the version.</li><li>Response persona Alfred by default (polite, dry sarcasm serving clarity, absolute technical precision); <code>/persona</code> switches Alfred/neutral. Dual vehicle: appended system prompt plus a hidden first-turn conversation message, because weak-adherence models ignore late system additions.</li></ul>\n<h4>System-role diagnostics</h4>\n<ul><li>Root-caused and fixed: OpenAI-compatible backends (ollama with some models, other proxies) silently drop \"developer\"-role messages. pi sends reasoning models' system prompt with that role, so the whole system prompt (pi base, personas, domains, budget notes) never reached the model. Fix: compat.supportsDeveloperRole=false (applied to presets for ollama, ollama-cloud, lmstudio, vllm, sglang, litellm, and new ollama registrations).</li><li>Doctor now probes this live (two ~10-token completions) and warns with the exact fix when a reasoning model behind such a backend lacks the flag.</li></ul>\n<h4>Platform</h4>\n<ul><li>Statusline segment with active model and auth state.</li><li>Zero runtime dependencies; pure-Node <code>lib/</code> modules; unit tests + CI (GitHub Actions: tests + headless doctor smoke + command-dispatch regression).</li></ul></section>";

  var veil, lastFocus

  function build() {
    if (document.getElementById("changelog-modal")) return
    veil = document.createElement("div")
    veil.className = "modal-veil"
    veil.id = "changelog-modal"
    veil.hidden = true
    var closeLabel = document.documentElement.lang === "en" ? "Close" : "Cerrar"
    var tag = document.documentElement.lang === "en" ? "House diary" : "Diario de la casa"
    veil.innerHTML =
      '<div class="modal" role="dialog" aria-modal="true" aria-labelledby="cl-title">' +
        '<div class="modal-head">' +
          '<span class="tag">' + tag + '</span>' +
          '<h2 id="cl-title">CHANGELOG</h2>' +
          '<button class="modal-close" type="button" aria-label="' + closeLabel + '">&times;</button>' +
        '</div>' +
        '<div class="modal-body">' + CHANGELOG_HTML + '</div>' +
      '</div>'
    document.body.appendChild(veil)
    veil.addEventListener("click", function (e) { if (e.target === veil) close() })
    veil.querySelector(".modal-close").addEventListener("click", close)
    document.addEventListener("keydown", function (e) {
      if (e.key === "Escape" && !veil.hidden) close()
    })
  }

  function open() {
    build()
    lastFocus = document.activeElement
    veil.hidden = false
    document.body.style.overflow = "hidden"
    veil.querySelector(".modal-close").focus()
  }

  function close() {
    veil.hidden = true
    document.body.style.overflow = ""
    if (lastFocus) lastFocus.focus()
  }

  document.querySelectorAll(".ver-btn").forEach(function (btn) {
    btn.addEventListener("click", open)
  })
})()
