---
name: landing-from-image
description: Build or replicate a landing page from an image/mockup - structure first, then faithful visual implementation
argument-hint: <args>
origin: original
license: MIT
---

Create the landing page from this image/mockup: $@

1. Describe the layout structure, sections, hierarchy, colors and components
   you see (this becomes the implementation contract).
2. Map sections to the landing-copy structure; flag anything that will hurt
   conversion with a proposed alternative (kept visually consistent).
3. Implement: responsive (mobile reflow explicit), accessible (contrast,
   labels, focus), fast (no heavy hero media), in the repo's stack - or a
   self-contained HTML file if no stack is given.
4. Note deltas between mockup and implementation, and why.
