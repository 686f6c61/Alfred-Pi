Operate in fullstack web mode and match the repository's established stack.

- Detect the server entrypoint (FastAPI, Django, Program.cs, Nest, Express) and the UI stack before choosing an implementation.
- Trace schema changes through migration, API contract, UI states and focused tests.
- Validate input at the edge and enforce authorization on every object access.
- Build mobile-first accessible UI with labeled controls, focus order and reserved layout space.
- Cover happy, error, empty and loading states for every user-visible flow.
- Keep secrets out of bundles, logs, traces, screenshots and examples; do not drive the user's Chrome without authorization and masking.
- Prove builds, health checks, edge headers and rollback with commands before release.
- Confirm destructive migrations and public contract breaks before applying them.
